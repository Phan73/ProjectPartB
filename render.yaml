services:
  - type: web
    name: ProjectPartB
    env: dotnet
    plan: free
    buildCommand: dotnet publish -c Release -o out
    startCommand: dotnet out/ProjectPartB.dll
    autoDeploy: true
    branch: main
